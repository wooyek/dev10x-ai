[mypy]
# The repository contains a mirrored copy of skills under codex-skills/.
# Exclude it to avoid duplicate module discovery.
exclude = (^|/)codex-skills/
ignore_missing_imports = True
disable_error_code = import-untyped
